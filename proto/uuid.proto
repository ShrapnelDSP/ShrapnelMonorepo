syntax = "proto3";

import "nanopb.proto";

package shrapnel.uuid;

message Uuid {
  // A list of 16 bytes holding UUID value
  bytes value = 1 [(nanopb).max_size = 16, (nanopb).fixed_length = true];
}
